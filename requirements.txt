# Minimal runtime requirements for SmarterMarketScanner
# Includes libraries directly imported by the project source files.

numpy==2.4.6
pandas==3.0.3
requests==2.34.2
python-dotenv==1.0.0
ib-insync==0.9.86
yfinance==0.2.27
transformers==5.9.0
tokenizers==0.22.2
onnxruntime==1.16.1
tqdm==4.67.3
urllib3==2.7.0

# Optional (dev) - keep commented out if you don't need model conversion
# torch==2.12.0+cpu  # needed only to run finbert_to_onnx.py
